export const API_URL = process.env.API_URL as string;

export const routes = {
  default: "/",
  register: "/register",
  explore: "/explore",
  contacts: "/contacts",
  ads: "/ads",
};

export const apiRoutes = {
  default: "/",
  register: "/register",
  chats: "/chats",
  contacts: "/contacts",
}
