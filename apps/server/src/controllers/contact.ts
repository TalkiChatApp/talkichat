import { Request, Response } from "express";

export const getContacts = async (req: Request, res: Response, next: Function) => {
    //
};

export const getContactById = async (req: Request, res: Response, next: Function) => {
  //
};

export const addContacts = async (req: Request, res: Response, next: Function) => {
    //
};

export const createContact = async (
  req: Request,
  res: Response,
  next: Function
) => {
  //
};

export const updateContact = async (
  req: Request,
  res: Response,
  next: Function
) => {
  //
};

export const deleteContact = async (
  req: Request,
  res: Response,
  next: Function
) => {
  //
};
